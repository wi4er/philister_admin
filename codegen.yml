schema:
  - "http://localhost:3000/graphql"
documents:
  - "src/graph/**/*.graphql"

config: { }
generates:
  src/graph/types.ts:
    config: { }
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
require: [ ]
watch: true
overwrite: true
