schema:
  - "http://localhost:8080/graphql"
documents:
  - "src/app/query/**/*.graphql"

config: { }
generates:
  src/app/query/types.ts:
    config: { }
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
require: [ ]
watch: true
overwrite: true
