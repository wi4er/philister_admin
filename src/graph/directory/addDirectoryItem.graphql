mutation AddDirectoryItem($item: DirectoryInput!) {
  directory {
    add(item: $item) {
      id
      value {
        id
      }
      property {
        string
        property {
          id
        }
      }
    }
  }
}
