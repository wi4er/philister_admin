query GetDirectoryEdit($id: String!) {
  property {
    list {
      id
    }
  }
  directory {
    item(id: $id) {
      id
      property {
        string
        property {
          id
        }
      }
      value {
        id
      }
    }
  }
}
