mutation updateDirectoryItem($item: DirectoryInput!) {
  directory {
    update(item: $item) {
      id
      value {
        id
      }
      property {
        value
        property {
          id
        }
      }
    }
  }
}
